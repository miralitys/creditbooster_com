services:
  - type: web
    name: creditbooster-com
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: GHL_WEBHOOK_URL
        sync: false
